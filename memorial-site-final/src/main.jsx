import React from "react";
import ReactDOM from "react-dom/client";
import FuneralWebsite from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FuneralWebsite />
  </React.StrictMode>
);
